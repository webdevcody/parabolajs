{
  "name": "parabolajs",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "cd packages/example && bun run dev",
    "example": "cd packages/example && bun run start"
  }
}
